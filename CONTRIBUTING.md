# Contributing

欢迎通过Issue和Pull Request改进D-CHyFLO。

1. 从`main`创建功能分支；
2. 不提交真实水库受限数据、个人信息、访问令牌或第三方数据库副本；
3. 新模型必须说明单位、系统边界、适用范围和失败条件；
4. 新功能必须配套合成测试；
5. 提交前运行`pytest`和`python scripts/audit_release.py`。

任何声称“最优容量”“工程安全”“实际减排量”或“未来预测”的变更，必须提供独立证据并更新声明边界。
